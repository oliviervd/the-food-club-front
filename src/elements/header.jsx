import React from "react";
import Logo from "./logo.jsx";

const Header = (props) => {
    return(
        <div className={"header--container"}>
            <Logo></Logo>
        </div>
    )
}

export default Header